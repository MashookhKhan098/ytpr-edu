name: Deploy to VPS (Docker Compose)

on:
  push:
    branches:
      - main   # change to your default branch (or keep as 'master' if that's correct)

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout code (only for context, not actually used in deployment)
      - name: Checkout code
        uses: actions/checkout@v4

      # 2️⃣ Setup SSH Agent
      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.VPS_PRIVATE_KEY }}

      # 3️⃣ SSH into VPS and deploy using docker-compose
      - name: Deploy via Docker Compose
        env:
          BRANCH_NAME: ${{ github.ref_name }}   # <-- Auto-detect branch name
        run: |
          echo "🚀 Deploying branch: $BRANCH_NAME"

          ssh -o StrictHostKeyChecking=no deployer@72.60.96.201 << 'EOF'
            set -e

            echo "📂 Moving to project folder..."
            cd /home/deployer/nextjs-apps/ytpr-edu

            echo "🔄 Pulling latest code from GitHub..."
            git fetch origin $BRANCH_NAME
            git reset --hard origin/$BRANCH_NAME

            echo "🐳 Rebuilding and restarting containers with docker-compose..."
            docker compose down
            docker compose up -d --build

            echo "✅ Deployment completed successfully!"
          EOF
